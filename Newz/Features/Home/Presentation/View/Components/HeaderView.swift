import SwiftUI

/// Header view.
struct HeaderView: View {
    var body: some View {
        Text("Today's News")
            .font(.largeTitle)
            .fontWeight(.semibold)
            .foregroundColor(.black)
       }
   }
